packages:
  - "packages/*"
  - "packages/integrations/*"
  - "apps/*"

# lefthook postinstall links its binary; approve so install doesn't hard-fail.
allowBuilds:
  esbuild: true
  lefthook: true

verifyDepsBeforeRun: false
